<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
    <act_window id="action_lcr_mdl"
        name="All customers"
        res_model="account.payment.order"
        view_mode="tree,form"
        limit="80"
        multi="True"
        key2="client_action_multi" />

        domain="[('customer', '=', True)]"
        context="{'default_customer': True}"

    <record id="dditem1_action" model="ir.actions.server">
            <field name="name">Text to display as menu item</field>
            <field name="model_id" ref="account_payment_order.model_account_payment_order"/>
            <field name="state">code</field>
            <field name="condition">True</field>
            <field name="code">if object: self.browse(cr,uid,context.get('active_ids', [])).your_function()</field>
    </record>
    -->

    <!-- Action confirm_payments for multi -->
    <record id="action_lcr_mdl" model="ir.actions.server">
        <field name="name">Téléchager fichier CFONB</field>
        <field name="type">ir.actions.server</field>
        <field name="state">code</field>
        <field name="model_id" ref="account_payment_order.model_account_payment_order"/>
        <field name="binding_model_id" ref="account_payment_order.model_account_payment_order"/>
        <field name="code">
            action = records.gen_cfonb_multi()
        </field>
    </record>

<!--
    <record id="dditem1" model="ir.values">
            <field name="name">name</field>
            <field name="model">account.payment.order</field>
            <field name="key" >action</field>
            <field name="key2">client_action_multi</field>
            <field name="value" eval="'ir.actions.server,%d'%dditem1_action"/>
    </record>
    -->

</odoo>
