<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <record id="yziact_departement_form" model="ir.ui.view">
        <field name="name">Departement</field>
        <field name="model">yziact.departement</field>
        <field name="arch" type="xml">
            <form name="main_form">
                <sheet>
                    <group>
                        <field name="number"/>
                        <field name="name"/>
                    </group>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread" options="{'thread_level': 1}"/>
                </div>
            </form>
        </field>
    </record>

    <record id="yziact_departement_tree" model="ir.ui.view">
        <field name="name">Departement</field>
        <field name="model">yziact.departement</field>
        <field name="arch" type="xml">
            <tree name="main_tree">
                <field name="number"/>
                <field name="name"/>
            </tree>
        </field>
    </record>

    <record id="yziact_departement_action_view" model="ir.actions.act_window">
        <field name="name">Departement</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">yziact.departement</field>
        <field name="view_mode">tree,form</field>
    </record>

    <!-- This Menu Item must have a parent and an action -->
    <menuitem id="yziact_departement_menu" name="Departement" parent="sale.menu_sale_config" action="yziact_departement_action_view" sequence="10"/>
</odoo>
