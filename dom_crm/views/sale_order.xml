<?xml version="1.0" encoding="UTF-8" ?>

<odoo>

    <!--SALE_REPORT-->
    <record id="dom_action_order_report_self_only" model="ir.actions.act_window">
        <field name="name">Rapport des Ventes</field>
        <field name="res_model">sale.report</field>
        <field name="view_type">form</field>
        <field name="view_mode">graph,pivot</field>
        <field name="view_id"></field>  <!-- force empty -->
        <field name="search_view_id" ref="sale.view_order_product_search"/>
        <field name="domain">[('user_id','=',uid)]</field>
        <field name="context">{'search_default_Sales':1, 'group_by_no_leaf':1,'group_by':[]}</field>
        <field name="help">This report performs analysis on your quotations and sales orders. Analysis check your sales revenues and sort it by different group criteria (salesman, partner, product, etc.) Use this report to perform analysis on sales not having invoiced yet. If you want to analyse your turnover, you should use the Invoice Analysis report in the Accounting application.</field>
    </record>


    <menuitem id="dom_menu_report_product_self_only"
            name="Vente"
            action="dom_action_order_report_self_only"
            parent="sale.menu_sale_report"
            sequence="1"/>

</odoo>
