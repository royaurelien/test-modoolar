<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Inherit Form View to Modify it -->
    <record id="dom_partner_form" model="ir.ui.view">
        <field name="name">Contacts</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form"/>
        <field name="arch" type="xml">


            <xpath expr="//sheet/group" position="after">
                <group>
                    <group>
                        <field name="famille" />
                        <field name="contact1" options="{'no_create':True}"/>
                        <field name="contact2" options="{'no_create':True}"/>
                        <field name="contact3" options="{'no_create':True}"/>
                    </group>
                    <group>
                        <field name="ca_12" />
                        <field name="freq_contact"/>
                        <!--<field name="plv" />-->
                        <field name="plv_id" widget="many2many_tags" />
                        <!--<field name="presentoir" />-->
                        <field name="presentoir_id" widget="many2many_tags"/>
                        <!--<field name="plv_pdt"/>
                        <field name="plv_p_int"/>
                        <field name="plv_p_ext"/>
                        <field name="plv_p_ext"/>
                        <field name="plv_carrelage"/>-->
                    </group>
                </group>
            </xpath>

            <xpath expr="//notebook/page[@name='internal_notes']/field[@name='comment']" position="before">
                <group>
                    <field name="taux_commission"/>
                </group>
            </xpath>

            <xpath expr="//notebook/page[@name='sales_purchases']/group[@name='container_row_2']" position="inside">
                <group>
                    <field name="code_api" />
                    <field name="horaires_livraison"/>
                    <!--<field name="transporteur"/>-->
                    <field name="url_bfa" />
                    <field name="bfa" />
                </group>
            </xpath>

            <!--Modif de l'affichage de l'onglet contact-->

            <!-- Contacts & Adresses -->
            <xpath expr="//field[@name='child_ids']" position="attributes">
                <attribute name="domain">[('company_type','=','person')]</attribute>
                <attribute name="context">
                    {'default_parent_id': active_id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_supplier': supplier, 'default_customer': customer, 'default_lang': lang,'default_company_type': 'person', 'default_type_company': False, 'default_is_company': False}
                </attribute>
            </xpath>

            <!-- Sociétés liées -->
            <xpath expr="//page[@autofocus='autofocus']" position="after">
                <page name="company" string="Sociétés Liées" attrs="{'invisible': [('is_company','=', False)]}">
                    <field name="child_ids_2"
                           mode="kanban"
                           domain="[('company_type','=','company')]"
                           options="{'no_quick_create': True}"
                           context="{'default_parent_id': active_id, 'default_street': street, 'default_street2': street2, 'default_city': city, 'default_state_id': state_id, 'default_zip': zip, 'default_country_id': country_id, 'default_supplier': supplier, 'default_customer': customer, 'default_lang': lang,'default_company_type': 'company', 'default_type_company':True, 'default_is_company':True}">
                        <kanban>
                            <field name="company_type"/>
                            <field name="color"/>
                            <field name="name"/>
                            <field name="title"/>
                            <field name="type"/>
                            <field name="email"/>
                            <field name="parent_id"/>
                            <field name="is_company"/>
                            <field name="function"/>
                            <field name="phone"/>
                            <field name="street"/>
                            <field name="street2"/>
                            <field name="zip"/>
                            <field name="city"/>
                            <field name="country_id"/>
                            <field name="mobile"/>
                            <field name="fax"/>
                            <field name="state_id"/>
                            <field name="image"/>
                            <field name="lang"/>
                            <templates>
                                <t t-name="kanban-box">
                                    <!--<t t-if="record.company_type.raw_value == 'company'">-->
                                    <t t-set="color" t-value="kanban_color(record.color.raw_value)"/>
                                    <div t-att-class="color + (record.title.raw_value == 1 ? ' oe_kanban_color_alert' : '') + ' oe_kanban_global_click'">
                                        <a t-if="!read_only_mode" type="delete" class="fa fa-times pull-right"/>
                                        <div class="o_kanban_image">
                                            <img t-if="record.image.raw_value" t-att-src="'data:image/png;base64,'+record.image.raw_value"/>
                                            <t t-if="!record.image.raw_value">
                                                <img t-if="record.type.raw_value === 'delivery'" t-att-src="_s + &quot;/base/static/src/img/truck.png&quot;"/>
                                                <img t-if="record.type.raw_value === 'invoice'" t-att-src="_s + &quot;/base/static/src/img/money.png&quot;"/>
                                                <t t-if="record.type.raw_value !== 'invoice' &amp;&amp; record.type.raw_value !== 'delivery'">
                                                    <img t-if="record.is_company.raw_value === true" t-att-src="_s + &quot;/base/static/src/img/company_image.png&quot;"/>
                                                    <img t-if="record.is_company.raw_value === false" t-att-src="_s + &quot;/base/static/src/img/avatar.png&quot;"/>
                                                </t>
                                            </t>
                                        </div>
                                        <div class="oe_kanban_details">
                                            <field name="name"/>
                                            <div t-if="record.function.raw_value"><field name="function"/></div>
                                            <div t-if="record.email.raw_value"><field name="email"/></div>
                                            <div t-if="record.type.raw_value != 'contact'">
                                                <div>
                                                    <field name="zip"/>
                                                    <field name="city"/>
                                                </div>
                                                <field t-if="record.state_id.raw_value" name="state_id"/>
                                                <field name="country_id"/>
                                            </div>
                                            <div t-if="record.phone.raw_value">Phone: <field name="phone"/></div>
                                            <div t-if="record.mobile.raw_value">Mobile: <field name="mobile"/></div>
                                            <div t-if="record.fax.raw_value">Fax: <field name="fax"/></div>
                                        </div>
                                    </div>
                                    <!--</t>-->
                                </t>
                            </templates>
                        </kanban>
                    </field>
                </page>
            </xpath>

            <xpath expr="//field[@name='user_id']" position="attributes">
                <attribute name="string">Commercial</attribute>
                <attribute name="attrs">{'required':[('type_rel','=','Client')]}</attribute>
            </xpath>
            <xpath expr="//field[@name='category_id']" position="before">
                <field name="dep_id"/>
            </xpath>

            <xpath expr="//button[@name='open_parent']" position="attributes">
                <attribute name="attrs">{'invisible': ['|','|',('parent_id', '=', False),('company_type', '!=', 'person'), ('type', '!=', 'contact')]}</attribute>
            </xpath>

            <xpath expr="//field[@name='street']" position="attributes">
                <attribute name="attrs">{'readonly': [('type', '=', 'contact'),('company_type', '=', 'person'),('parent_id', '!=', False)]}</attribute>
            </xpath>

            <xpath expr="//field[@name='street2']" position="attributes">
                <attribute name="attrs">{'readonly': [('type', '=', 'contact'),('company_type', '=', 'person'),('parent_id', '!=', False)]}</attribute>
            </xpath>

            <xpath expr="//field[@name='city']" position="attributes">
                <attribute name="attrs">{'readonly': [('type', '=', 'contact'),('company_type', '=', 'person'),('parent_id', '!=', False)]}</attribute>
            </xpath>

            <xpath expr="//field[@name='state_id']" position="attributes">
                <attribute name="attrs">{'readonly': [('type', '=', 'contact'),('company_type', '=', 'person'),('parent_id', '!=', False)]}</attribute>
            </xpath>

            <xpath expr="//field[@name='zip']" position="attributes">
                <attribute name="attrs">{'readonly': [('type', '=', 'contact'),('company_type', '=', 'person'),('parent_id', '!=', False)]}</attribute>
            </xpath>

            <xpath expr="//field[@name='country_id']" position="attributes">
                <attribute name="attrs">{'readonly': [('type', '=', 'contact'),('company_type', '=', 'person'),('parent_id', '!=', False)]}</attribute>
            </xpath>

             <xpath expr="//field[@name='parent_id']" position="attributes">
                     <attribute name="invisible">False</attribute>
             </xpath>
             <xpath expr="//field[@name='state_id']" position="attributes">
                     <attribute name="placeholder">Région</attribute>
             </xpath>

            <xpath expr="//group[@name='purchase']/field[@name='supplier']" position="after">
                <field name="famille_fournisseur"/>
            </xpath>
        </field>
    </record>

    <!-- Inherit Form View to Modify it -->
    <record id="dom_partner_crm_inherit" model="ir.ui.view">
        <field name="name">Contacts</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="crm_yzi.crm_res_partner_inherit"/>
        <field name="arch" type="xml">

            <xpath expr="//field[@name='type_rel']" position="after">
                <field name="type_tier"/>
            </xpath>

        </field>
    </record>

    <record id="dom_partner_account_form" model="ir.ui.view">
        <field name="name">Contacts</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="account.view_partner_property_form"/>
        <field name="arch" type="xml">

            <xpath expr="//notebook/page[@name='accounting']/group" position="inside">
                <group>
                    <field name="paie_livraison"/>
                    <field name="fac_mail"/>
                </group>
            </xpath>

        </field>
    </record>

    <record id="dom_partner_filter" model="ir.ui.view">
        <field name="name">dom.partner.select</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_res_partner_filter"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']" position="after">
                <field name="dep_id" string="Département"/>
            </xpath>
        </field>
    </record>
</odoo>
